;(function(){
  $('#star-tags')
    .html('<%= j render "tag_form" %>')
    .modal()
    .find('.tm-input')
      .tagsManager({
        prefilled: <%= raw @tags.map(&:name).to_json %>
      })
      .focus();
})();
